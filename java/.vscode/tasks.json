{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile-Recursion",
            "type": "shell",
            "command": "javac -d ${workspaceFolder}/recursion/bin ${workspaceFolder}/recursion/*.java"
        },
        {
            "label": "Compile-Array",
            "type": "shell",
            "command": "javac -d ${workspaceFolder}/arrays/bin ${workspaceFolder}/arrays/*.java"
        }
    ]
}